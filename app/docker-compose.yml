services:

  backend:
    build:
      context: ./BackEnd
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ../my-react-app
    ports:
      - "80:80"
    restart: unless-stopped